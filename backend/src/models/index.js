const productModel = require('./product.Model');
const salesModel = require('./sales.Model');
const salesProductModel = require('./salesAndProducts.Model');

module.exports = {
  productModel,
  salesModel,
  salesProductModel,
};